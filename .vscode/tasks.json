{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build gpxtools",
            "type": "shell",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceFolder}/build",
                "-G", 
                "Unix Makefiles",
                "-DCMAKE_BUILD_TYPE=Debug"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
